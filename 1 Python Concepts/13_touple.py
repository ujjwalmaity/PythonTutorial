# list is mutable but touple is immutable (can't be changed)
# [] -> braces
# {} -> curly braces
# () -> parenthesis
# <> -> Angular Bracket

fruits = ('Apple', 'Mango', 'Peach')
print(fruits)
print(fruits[0])

fruits = 'Apple', 'Mango', 'Peach'
print(fruits)
